<Window x:Class="ChangeLens.DiffDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Lens" Height="160" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 古いモデル選択 -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,15" VerticalAlignment="Center">
            <TextBox x:Name="TxtExcelPath" Width="260" Height="28" IsReadOnly="True" VerticalContentAlignment="Center"/>
            <Button Content="reference…" Width="80" Height="28" Margin="10,0,0,0" Click="SelectFile_Click"/>
        </StackPanel>

        <!-- 操作ボタン -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Left">
            <Button Content="start" Width="120" Height="35" Click="BtnStart_Click"/>
            <Button Content="reset color" Width="120" Height="35" Margin="15,0,0,0" Click="BtnResetColor_Click"/>
        </StackPanel>
    </Grid>
</Window>
